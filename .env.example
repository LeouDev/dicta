# Supabase project settings → API. Both values are public by design; row level
# security is what protects data. Use the publishable key (sb_publishable_…)
# or the legacy anon JWT. NEVER put a secret / service_role key here.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
