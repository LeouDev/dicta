/**
 * The small part of the card engine the post page needs (no Skia): reading a
 * stored design the way the app does, and the card's size.
 */
export { cardSize } from '@/features/quote-card/geometry';
export { parseQuoteDesign } from '@/features/quote-card/serialize';
